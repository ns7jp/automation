#!/bin/bash
#===============================================================================
# ex02: 入力チェックと安全装置(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所を埋めれば完成します。
#
# 採点: exercises ディレクトリで ./check.sh 02
#===============================================================================

# set -u : 未定義の変数を使ったらエラーで止める(タイプミス検知のため)
set -u

#-------------------------------------------------------------------------------
# 使い方を表示する関数(ここは完成しています)
#-------------------------------------------------------------------------------
usage() {
    echo "使い方: $(basename "$0") <入力ファイル>"
    echo "  指定したファイルが、これから処理できる状態かどうかを確認します。"
}

#-------------------------------------------------------------------------------
# TODO 1: 引数がちょうど1個でなければ、エラーメッセージと usage を
#         「標準エラー出力」に表示して、終了ステータス 1 で終わる
#   ヒント: 引数の個数は $# 。「等しくない」は -ne 。標準エラー出力は >&2
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 2: 1個目の引数を、チェック対象のファイルとして変数に入れる
#-------------------------------------------------------------------------------
INPUT_FILE=""

#-------------------------------------------------------------------------------
# TODO 3: ファイルが存在しないときは、標準エラー出力に
#           エラー: ファイルが見つかりません: <パス>
#         を表示して、終了ステータス 2 で終わる
#   ヒント: [[ ! -f "$INPUT_FILE" ]] で「通常ファイルとして存在しない」を判定できる
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 4: 読み取り権限が無いときは、標準エラー出力に
#           エラー: 読み取り権限がありません: <パス>
#         を表示して、終了ステータス 3 で終わる
#   ヒント: 読み取り権限の判定は -r
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 5: 中身が空(サイズ0)のときは、標準エラー出力に
#           エラー: ファイルが空です: <パス>
#         を表示して、終了ステータス 4 で終わる
#   ヒント: -s は「サイズが0より大きい」。空のときは ! を付けて否定する
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 6: 行数を数えて変数に入れる
#   ヒント: $(wc -l < "$INPUT_FILE") と書くと数字だけが得られる
#           ( wc -l "$INPUT_FILE" だとファイル名まで出てしまう )
#-------------------------------------------------------------------------------
LINE_COUNT=""

#-------------------------------------------------------------------------------
# TODO 7: 仕様どおりのメッセージを「標準出力」に表示する
#           OK: <パス> は処理できます (<行数>行)
#-------------------------------------------------------------------------------
echo "OK: ${INPUT_FILE}"

# ここまで来たら入力に問題は無い。成功を表す終了ステータス0で終わる。
exit 0

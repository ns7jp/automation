#!/bin/bash
#===============================================================================
# ex13: 前回の続きから読む(差分処理)(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所を埋めれば完成します。
#
# 採点: exercises ディレクトリで ./check.sh 13
#===============================================================================

# set -u : 未定義の変数を使ったらエラーで止める(タイプミス検知のため)
set -u

#-------------------------------------------------------------------------------
# 使い方を表示する関数(ここは完成済み。手を入れる必要はない)
#-------------------------------------------------------------------------------
usage() {
    echo "使い方: $(basename "$0") <ログファイル>"
    echo "  前回の続きから、増えた行だけを読み取って表示します。"
}

#-------------------------------------------------------------------------------
# TODO 1: 引数がちょうど1個でなければ、次の2つを「標準エラー出力」に表示して
#         終了ステータス1で終わる
#           エラー: ログファイルを1つ指定してください。
#           (続けて usage の内容)
#   ヒント: 引数の個数は $# 。標準エラー出力に出すには >&2 を付ける
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 2: 1個目の引数をログファイルのパスとして変数に入れる
#   ヒント: ${1:-} は「$1 が未定義なら空文字」の意味。TODO 1 を書いたあとなら
#           "$1" と書いてよい
#-------------------------------------------------------------------------------
LOG_FILE="${1:-}"

#-------------------------------------------------------------------------------
# TODO 3: ログファイルが存在しなければ、次のメッセージを「標準エラー出力」に
#         表示して終了ステータス1で終わる
#           エラー: ログファイルが見つかりません: <指定されたパス>
#   ヒント: [[ ! -f "$LOG_FILE" ]] で「通常ファイルとして存在しない」を判定できる
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 4: 状態ファイルのパスを組み立てる
#         <ログファイルのパス>.offset にする(例: app.log → app.log.offset)
#   ヒント: 変数のうしろに文字列をつなげるだけ。"${LOG_FILE}.offset"
#-------------------------------------------------------------------------------
STATE_FILE=""

#-------------------------------------------------------------------------------
# TODO 5: 前回までに処理した行数を LAST_LINE に入れる
#         状態ファイルが無ければ 0 のままにする
#   ヒント: if [[ -f "$STATE_FILE" ]]; then LAST_LINE="$(cat "$STATE_FILE")"; fi
#-------------------------------------------------------------------------------
LAST_LINE=0

#-------------------------------------------------------------------------------
# TODO 6: 今のログファイルの行数を数えて TOTAL_LINES に入れる
#   ヒント: wc -l < ファイル と書くと、ファイル名が付かず数字だけが得られる
#-------------------------------------------------------------------------------
TOTAL_LINES=0

#-------------------------------------------------------------------------------
# TODO 7: ログローテートの検知
#         今の行数が前回の行数より少なければ、次のメッセージを「標準出力」に
#         表示してから LAST_LINE を 0 に戻す(=先頭から読み直す)
#           ログがローテートされたため先頭から読み直します
#   ヒント: 数値の比較は [[ "$TOTAL_LINES" -lt "$LAST_LINE" ]]
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 8: 新規行の件数を計算する(今の行数 - 前回の行数)
#   ヒント: 算術式は $(( ))
#-------------------------------------------------------------------------------
NEW_COUNT=0

#-------------------------------------------------------------------------------
# TODO 9: 新規行が1行以上あるなら、「前回の行数 + 1」行目から最後までを
#         1行ずつ「新規行: <行の内容>」の形式で標準出力に表示する
#   ヒント: 読み始める行番号は $((LAST_LINE + 1))
#           tail -n "+<行番号>" "$LOG_FILE" で「その行から最後まで」を取り出せる
#           1行ずつ処理するには while IFS= read -r line; do ... done < <(コマンド)
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 10: まとめの1行を表示する
#          新規: <件数>行 (総行数: <総行数>行)
#   下の echo は仮の出力なので、変数を使って正しい値が出るように直すこと
#-------------------------------------------------------------------------------
echo "新規: 0行 (総行数: 0行)"

#-------------------------------------------------------------------------------
# TODO 11: 状態ファイルを「今の総行数」で上書きする
#   ヒント: printf '%s\n' "$TOTAL_LINES" > "$STATE_FILE"
#           追記(>>)ではなく上書き(>)を使う
#-------------------------------------------------------------------------------


# 正常終了(成功を表す終了ステータス0を返す)
exit 0

---
# =====================================================================
# site.yml
# ex18: Playbookを完成させる(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所に設定を書き、書き終えたら TODO を含む行はすべて消してください。
# この案内文の行も TODO を含むので、一緒に消すのを忘れずに。
#
# 採点: exercises ディレクトリで ./check.sh 18
#
# YAMLの書式ルール(YAMLは「見た目」がそのまま意味になる言語です)
#   - インデントは半角スペース2つ。タブは絶対に使えない
#   - 行頭の「- 」はリストの1要素という意味
#   - 「キー: 値」のコロンの後ろには半角スペースを1つ入れる
#   - # で始まる行はコメント
#   - 記号はすべて半角。全角のコロンやスペースが混ざると読み込めない
# =====================================================================

# Playbook の一番外側は「play(プレイ)のリスト」。
# 「- 」で始まっているのは、この play がリストの1要素だから。
# この play の中身は、すべて2スペース下げて書く。
- name: Webサーバーの初期設定

  # TODO 1: この play を適用する対象のホストグループを指定する
  #   値: webservers
  #   ヒント: キーは hosts 。インベントリ(対象サーバーの一覧)に書いた
  #           グループ名を指定する


  # TODO 2: この play のタスクを管理者権限(sudo)で実行する
  #   ヒント: キーは become 、値は true


  # TODO 3: この play の中で使う変数を定義する
  #   nginx_port という変数に 80 を入れる
  #   ヒント: vars: と書き、その2スペース下に nginx_port: 80 と書く


  # TODO 4: tasks: と書き、その下に次の3つのタスクをこの順で並べる
  #   タスクは「- name: 説明」で始め、その1つ下の行に使うモジュール、
  #   さらにその下にモジュールの引数を並べる。
  #
  #   タスク1「nginx をインストールする」
  #     モジュール: ansible.builtin.apt
  #     引数      : name: nginx / state: present / update_cache: true
  #
  #   タスク2「設定ファイルを配置する」
  #     モジュール: ansible.builtin.template
  #     引数      : src: nginx.conf.j2 / dest: /etc/nginx/nginx.conf
  #                 owner: root / group: root / mode: "0644"
  #     さらに、モジュール名と同じ深さに notify: を書き、
  #     TODO 5 で作るハンドラと「まったく同じ名前」を指定する
  #
  #   タスク3「nginx を起動し自動起動を有効にする」
  #     モジュール: ansible.builtin.service
  #     引数      : name: nginx / state: started / enabled: true


  # TODO 5: handlers: と書き、その下にハンドラを1つ定義する
  #   ハンドラの書き方はタスクとまったく同じ。
  #     name      : nginx を再起動する
  #     モジュール: ansible.builtin.service
  #     引数      : name: nginx / state: restarted

#!/bin/bash
#===============================================================================
# ex05: オプション解析とドライラン(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所を埋めれば完成します。
#
# 採点: exercises ディレクトリで ./check.sh 05
#===============================================================================

# set -u : 未定義の変数を使ったらエラーで止める(タイプミス検知のため)
set -u

#-------------------------------------------------------------------------------
# 既定値
#   オプションで指定が無かったときに使う値を、先に変数へ入れておく。
#-------------------------------------------------------------------------------
SCRIPT_NAME="$(basename "$0")"
TARGET_DIR="./tmp"        # -d の既定値
EXT="tmp"                 # -e の既定値(ドットは付けない)
DRY_RUN="false"           # -n が指定されたら "true" にする

#-------------------------------------------------------------------------------
# 使い方を表示する関数
#-------------------------------------------------------------------------------
usage() {
    echo "使い方: ${SCRIPT_NAME} [-d <ディレクトリ>] [-e <拡張子>] [-n] [-h]"
    echo "  指定ディレクトリ直下の *.<拡張子> を削除します。"
    echo "  -d <ディレクトリ>  対象ディレクトリ (既定: ${TARGET_DIR})"
    echo "  -e <拡張子>        削除する拡張子   (既定: ${EXT})"
    echo "  -n                 ドライラン。実際には削除しません (--dry-run も可)"
    echo "  -h                 この使い方を表示します (--help も可)"
}

#-------------------------------------------------------------------------------
# TODO 1: 長いオプションを短いオプションに置き換える
#   --dry-run → -n、--help → -h に変換してから getopts に渡す。
#   ヒント: args=() に1つずつ詰め替えて、最後に set -- "${args[@]}" で入れ替える
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 2: getopts でオプションを解析する
#   -d <ディレクトリ> → TARGET_DIR に入れる
#   -e <拡張子>       → EXT に入れる
#   -n                → DRY_RUN="true"
#   -h                → usage を表示して exit 0(標準出力)
#   未定義のオプション → usage を標準エラー出力に出して exit 1
#   ヒント: while getopts "d:e:nh" opt; do case "$opt" in ... esac; done
#           値を取るオプションには : を付ける。値は $OPTARG に入る
#           最後に shift $((OPTIND - 1)) を書く
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 3: 対象ディレクトリが存在するか確認する
#   存在しなければ、次のメッセージを標準エラー出力に出して exit 1
#     エラー: ディレクトリが見つかりません: <パス>
#   ヒント: [[ ! -d "$TARGET_DIR" ]] で「ディレクトリとして存在しない」を判定できる
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 4: 対象ファイルを1件ずつ処理する
#   (1) ${TARGET_DIR%/}/*.${EXT} をループで回す
#   (2) [[ -f "$file" ]] || continue で、実在するファイル以外を飛ばす
#       (1件もマッチしないとき、パターン文字列がそのまま入ってくるため)
#   (3) basename でファイル名だけを取り出す
#   (4) 件数を数える  COUNT=$((COUNT + 1))
#   (5) DRY_RUN が "true" なら  [dry-run] 削除します: <ファイル名>
#       そうでなければ rm で削除して  削除しました: <ファイル名>
#-------------------------------------------------------------------------------
COUNT=0


#-------------------------------------------------------------------------------
# TODO 5: 件数を表示する
#   対象: <件数>件
#   ドライランのときは、続けて次の行も表示する
#   ドライランのため、実際には削除していません。
#-------------------------------------------------------------------------------


# 正常終了(削除件数が0件でも成功として終わる)
exit 0

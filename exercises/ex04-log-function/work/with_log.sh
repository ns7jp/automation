#!/bin/bash
#===============================================================================
# ex04: ログ関数と実行記録(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所を埋めれば完成します。
#
# 採点: exercises ディレクトリで ./check.sh 04
#===============================================================================

# set -u : 未定義の変数を使ったらエラーで止める(タイプミス検知のため)
set -u

#-------------------------------------------------------------------------------
# 既定値と集計用カウンタ
#   カウンタは必ず 0 で初期化しておく(空文字のままだと計算でエラーになる)。
#-------------------------------------------------------------------------------
SCRIPT_NAME="$(basename "$0")"
LOG_DIR="./logs"                              # -l の指定が無いときの出力先
TIMESTAMP="$(date '+%Y%m%d_%H%M%S')"          # ファイル名用の日時

INFO_COUNT=0
WARN_COUNT=0
ERROR_COUNT=0

#-------------------------------------------------------------------------------
# 使い方を表示する関数
#-------------------------------------------------------------------------------
usage() {
    echo "使い方: ${SCRIPT_NAME} [-l <ログ出力ディレクトリ>]"
    echo "  処理の実行記録を、画面とログファイルの両方に出力します。"
}

#-------------------------------------------------------------------------------
# TODO 1: -l <ディレクトリ> を受け取り、LOG_DIR を上書きする
#   未定義のオプション(例: -z)が来たら usage を標準エラー出力に出して exit 1
#   ヒント: while getopts "l:" opt; do case "$opt" in l) ... ;; \?) ... ;; esac; done
#           オプションに渡された値は $OPTARG に入る
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 2: ログ出力ディレクトリを作る
#   mkdir -p で作り、失敗したら次のメッセージを標準エラー出力に出して exit 1
#     エラー: ログディレクトリを作成できません: <パス>
#   ヒント: if ! mkdir -p "$LOG_DIR"; then ... fi
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 3: ログファイルのパスを組み立てる
#   形式: <ログ出力ディレクトリ>/with_log_<YYYYMMDD>_<HHMMSS>.log
#   ヒント: ${LOG_DIR%/} で末尾のスラッシュを取り除ける。日時は $TIMESTAMP
#-------------------------------------------------------------------------------
LOG_FILE=""

#-------------------------------------------------------------------------------
# TODO 4: ログ出力関数 log を完成させる
#   (1) レベルごとの件数を数える(INFO_COUNT / WARN_COUNT / ERROR_COUNT)
#       ヒント: case "$level" in INFO) INFO_COUNT=$((INFO_COUNT + 1)) ;; ... esac
#   (2) 次の書式で、画面とログファイルの両方に出す
#       [YYYY-MM-DD HH:MM:SS] [レベル] メッセージ
#       ヒント: echo "[${now}] [${level}] ${message}" | tee -a "$LOG_FILE"
#-------------------------------------------------------------------------------
log() {
    local level="$1"
    shift
    local message="$*"
    local now
    now="$(date '+%Y-%m-%d %H:%M:%S')"

    # ここに (1) と (2) を書く
    :
}

#-------------------------------------------------------------------------------
# TODO 5: 本体処理として次の5行を、この順番どおりに log で記録する
#   INFO  ===== 処理を開始します =====
#   INFO  対象を3件読み込みました
#   WARN  1件は対象外のためスキップしました
#   ERROR 1件の処理に失敗しました
#   INFO  ===== 処理を終了します =====
#   ヒント: log "INFO" "===== 処理を開始します ====="
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 6: サマリを log で1行出す(レベルは INFO)
#   INFO: <件数>件 / WARN: <件数>件 / ERROR: <件数>件
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 7: ERROR が1件以上なら終了ステータス1、無ければ0で終わる
#   ヒント: if [[ "$ERROR_COUNT" -gt 0 ]]; then exit 1; fi
#-------------------------------------------------------------------------------

exit 0

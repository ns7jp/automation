# =====================================================================
# backup_cron.txt
# ex10: cron定義を書く(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# 下にある5か所の指示コメントを、実際の設定1行に置き換えてください。
# (指示コメントの行そのものは、書き終えたら削除します)
#
# 採点: exercises ディレクトリで ./check.sh 10
#
# 書式: 分 時 日 月 曜日 実行するコマンド
#       分(0-59) 時(0-23) 日(1-31) 月(1-12) 曜日(0-7、0と7は日曜)
#       「*」は「その項目を絞り込まない=毎回」を意味する
# =====================================================================


# TODO 1: cron の実行結果メールの宛先を admin@example.com にする
#   ヒント: MAILTO="宛先" の形式で、行の先頭から1行だけ書く


# TODO 2: 毎日 深夜2時30分 に /opt/scripts/backup.sh を実行し、
#         標準出力と標準エラー出力の両方を /var/log/backup.log へ追記する
#   ヒント: 追記は >> 、標準エラー出力を標準出力に合流させるのは 2>&1
#   コマンドは必ず絶対パスで書き、この行の直前に日本語の説明コメントも書くこと


# TODO 3: 月曜から金曜の 9時から18時まで、毎時0分 に
#         /opt/scripts/health_check.sh を実行し、
#         出力の両方を /var/log/health_check.log へ追記する
#   ヒント: 範囲の指定は 9-18 や 1-5 のように「開始-終了」で書く
#   この行の直前に日本語の説明コメントも書くこと


# TODO 4: 5分ごと に /opt/scripts/log_watch.sh を実行し、出力は捨てる
#   ヒント: 「N分ごと」は */N 。捨てるのは > /dev/null 2>&1
#   この行の直前に日本語の説明コメントも書くこと


# TODO 5: 毎月1日の 3時0分 に
#         /opt/scripts/rotate_backups.sh -d /backup -g 7 を実行し、
#         出力の両方を /var/log/rotate.log へ追記する
#   ヒント: 「毎月1日」は 日フィールドを 1 にして、月と曜日を * にする
#   この行の直前に日本語の説明コメントも書くこと

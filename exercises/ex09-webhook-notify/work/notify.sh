#!/bin/bash
#===============================================================================
# ex09: 失敗時のWebhook通知(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所を埋めれば完成します。
#
# 採点: exercises ディレクトリで ./check.sh 09
#===============================================================================

# set -u : 未定義の変数を使ったらエラーで止める(タイプミス検知のため)
set -u

#-------------------------------------------------------------------------------
# 使い方を表示する関数
#-------------------------------------------------------------------------------
usage() {
    echo "使い方: $(basename "$0") <success|failure> <メッセージ>"
    echo "  実行結果を Webhook (Slackなど) へ通知します。"
    echo "  送信先は環境変数 WEBHOOK_URL で指定します。"
}

#-------------------------------------------------------------------------------
# TODO 1: 引数がちょうど2個でなければ、usage を「標準エラー出力」に表示して
#         終了ステータス1で終わる
#   ヒント: 引数の個数は $# 。標準エラー出力に出すには >&2 を付ける
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 2: 1個目の引数を通知の種別、2個目の引数をメッセージとして変数に入れる
#   ヒント: ${1:-} と書くと、引数が無いときも set -u でエラーにならない
#-------------------------------------------------------------------------------
STATUS=""
MESSAGE=""

#-------------------------------------------------------------------------------
# TODO 3: 種別が success なら LABEL に "[SUCCESS]"、failure なら "[FAILURE]" を入れる
#         どちらでもなければエラーメッセージと usage を標準エラー出力に出して
#         終了ステータス1で終わる
#   ヒント: case "$STATUS" in success) ... ;; failure) ... ;; *) ... ;; esac
#-------------------------------------------------------------------------------
LABEL=""

#-------------------------------------------------------------------------------
# TODO 4: 環境変数 WEBHOOK_URL が未設定または空なら、標準出力に
#         「WEBHOOK_URL が未設定のため通知をスキップします」と表示して
#         終了ステータス0で終わる(エラーにはしない)
#   ヒント: if [[ -z "${WEBHOOK_URL:-}" ]]; then ... fi
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 5: ホスト名を hostname コマンドで取得する
#   ヒント: コマンド置換 $( )
#-------------------------------------------------------------------------------
HOST_NAME=""

#-------------------------------------------------------------------------------
# TODO 6: メッセージに含まれるダブルクォートを \" にエスケープする
#   ヒント: ${MESSAGE//\"/\\\"} と書くと、すべての " を \" に置換できる
#-------------------------------------------------------------------------------
ESCAPED_MESSAGE=""

#-------------------------------------------------------------------------------
# TODO 7: 送信する本文(JSON)を組み立てる
#   {"text":"[FAILURE] web01: バックアップに失敗しました"} の形にする
#   ヒント: JSONの中のダブルクォートは \" と書いて打ち消す
#-------------------------------------------------------------------------------
PAYLOAD=""

#-------------------------------------------------------------------------------
# TODO 8: curl で Webhook に POST する。失敗したら
#         「エラー: 通知の送信に失敗しました」を標準エラー出力に出して
#         終了ステータス1で終わる
#   ヒント:
#     if ! curl -sS -X POST \
#             -H "Content-Type: application/json" \
#             -d "$PAYLOAD" \
#             "$WEBHOOK_URL" > /dev/null; then
#         ...
#     fi
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 9: 送信できたことを標準出力に表示する
#   通知を送信しました: [FAILURE] web01: バックアップに失敗しました
#-------------------------------------------------------------------------------


# 正常終了(成功を表す終了ステータス0を返す)
exit 0

#!/bin/bash
#===============================================================================
# ex17: awkで稼働率を集計する(あなたが完成させるファイル)
#
# 課題の詳細は ../README.md を読んでください。
# TODO と書かれた場所を埋めれば完成します。
#
# 採点: exercises ディレクトリで ./check.sh 17
#===============================================================================

# set -u : 未定義の変数を使ったらエラーで止める(タイプミス検知のため)
set -u

#-------------------------------------------------------------------------------
# 使い方を表示する関数
#-------------------------------------------------------------------------------
usage() {
    echo "使い方: $(basename "$0") <監視ログファイル> [<しきい値(%)>]"
    echo "  監視ログを集計し、ホストごとの稼働率レポートを表示します。"
    echo "  しきい値を省略した場合は 99 (%) として判定します。"
}

#-------------------------------------------------------------------------------
# TODO 1: 引数が1個または2個でなければ、usage を「標準エラー出力」に表示して
#         終了ステータス1で終わる
#   ヒント: 引数の個数は $# 。[[ "$#" -lt 1 || "$#" -gt 2 ]] で判定できる
#           標準エラー出力に出すには usage >&2 のように >&2 を付ける
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 2: 1個目の引数をログファイル名、2個目の引数をしきい値として変数に入れる
#         しきい値は省略できる。省略されたときは 99 を使う
#   ヒント: "${2:-99}" は「$2 が無ければ 99 を使う」という書き方
#-------------------------------------------------------------------------------
LOG_FILE=""
THRESHOLD=""

#-------------------------------------------------------------------------------
# TODO 3: ログファイルが存在しなければ「見つかりません」を含むエラーを
#         標準エラー出力に表示し、終了ステータス1で終わる
#   ヒント: ファイルの存在確認は [[ ! -f "$LOG_FILE" ]]
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 4: awk でホストごとに集計し、sort でホスト名の昇順に並べる
#
#   awk に渡すもの: -F',' (カンマ区切り) と -v th="$THRESHOLD" (しきい値)
#   awk の中でやること:
#     - 空行 /^[[:space:]]*$/ と コメント行 /^[[:space:]]*#/ は next で飛ばす
#     - total[$2]++ で総件数、$3 が "OK" のときだけ ok[$2]++ で成功件数を数える
#     - END ブロックで for (host in total) を回し、稼働率と判定を計算して
#       「ホスト名 OK件数 総件数 稼働率 判定」の形で1ホスト1行を出力する
#   出力例(この中間データを次の TODO 5 で整形する):
#     web01 10 10 100.0 正常
#     web02 9 10 90.0 要確認
#-------------------------------------------------------------------------------
STATS=""

#-------------------------------------------------------------------------------
# TODO 5: 見出し行を表示する
#   ホスト名  OK/総数  稼働率  判定
#   (区切りは半角スペース2つ)
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 6: TODO 4 で作った中間データを1行ずつ読み、printf で表の形に整える
#
#   書式: printf '%-6s %-6s %5.1f%%  判定: %s\n' "$host" "${ok}/${total}" "$rate" "$judge"
#   同時に次の3つを数える:
#     TOTAL_RECORDS … 総レコード数(各ホストの総件数の合計)
#     HOST_COUNT    … ホスト数
#     NG_HOSTS      … 判定が「要確認」だったホストの数
#   ヒント: while read -r host ok total rate judge; do ... done <<< "$STATS"
#           STATS が空のときに空行を1回読んでしまうので、
#           [[ -z "$host" ]] && continue で飛ばしておく
#-------------------------------------------------------------------------------
TOTAL_RECORDS=0
HOST_COUNT=0
NG_HOSTS=0


#-------------------------------------------------------------------------------
# TODO 7: サマリ行を表示する
#   集計対象: <総レコード数>件 / ホスト数: <ホスト数>件 / しきい値: <しきい値>%
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# TODO 8: しきい値未満のホストが1つでもあれば終了ステータス1、
#         1つも無ければ0で終わる
#   ヒント: if [[ "$NG_HOSTS" -gt 0 ]]; then ... fi
#-------------------------------------------------------------------------------
exit 0
